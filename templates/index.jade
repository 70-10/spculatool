extends ./layout.jade

block style
  style
    include css/index.css
    
block content
  section.hero
    .hero-content
      .container
        h1.title 
          ruby
            rb Condara
            rp (
            rt コンダラ
            rp )
        h2.subtitle you can do it.
        form(action="/combi" method="POST")
          p
            input.input(type='text', placeholder="メインテーマを入力してください" name='main_theme')
          input(type="hidden" name="level" value="strong")
          //- p.control.is-inline
          //-   label.radio.condara-level
          //-     input(type="radio" name="level" value="weak")
          //-     span 弱め
          //-   label.radio.condara-level
          //-     input(type="radio" name="level" value="normal" checked="checked")
          //-     span 普通
          //-   label.radio.condara-level
          //-     input(type="radio" name="level" value="strong")
          //-     span 強め
          p
             button.button.is-primary アイデアをつくる
        
          
